// we are writing a code that checks if a num is less than 100
//firstly we intialize to zero
//secondly the condition a  numbeer to the varible
// statement to keep adding until is 100 it terminates
//while loop to increment
var num=0;
while(num<100)
{
num+=1;
 console.log(num);

}
// we used for loop to decrement
 
for (let num1=200;
    num1>=100;num1--){
        console.log(num1);
    }
